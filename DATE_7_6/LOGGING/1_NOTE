로깅(Logging)
    프로그램이 실행될 때 일어나는 이벤트를 추적하기 위한 용도로 소스코드에 삽입하는 출력문을 작성하는 것. 결과로 발생하는 출력문을 로그 라고함.

로그 레벨: Debug> Info > Warning > Error > Critical (개발자는 주로 Debug 레벨)

로깅 관련 객체
    Logger : 파이썬 모듈 단위로 로거 생성. 모든 로거는 root 로거의 자식
    Handler: 로거에 의해 생성된 log record를
    Formatter: 로그 출력 포맷 결정
